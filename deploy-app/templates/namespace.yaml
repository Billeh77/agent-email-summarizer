{{- if .Values.namespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    name: {{ .Values.namespace }}
    gateway-access: "true"
    {{- include "email-summarizer-ui.labels" . | nindent 4 }}
{{- end }}
